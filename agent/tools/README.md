# Custom ADK Tools

## Tools

### 1. BigQuery Vector Search Tool
- Translates natural language → SQL VECTOR_SEARCH queries
- Queries land record embeddings
- Returns retrieved text with metadata

### 2. e-Sankhyiki MCP Tool
- FastMCP client for live MoSPI data
- Dynamic API request formulation
- Handles 4-step protocol (list → get → metadata → data)
